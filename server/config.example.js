/* ============================================
   GymFlow AI Server — 配置文件（示例）
   复制为 config.js 并填入真实信息
   config.js 不会被上传到 Git
   ============================================ */

module.exports = {
  // 智谱 API 密钥（从 https://open.bigmodel.cn 获取）
  apiKey: '你的智谱API密钥',

  // 全局访问密码（APK 请求时携带）
  password: 'gymflow2024',

  // 单设备每日最大调用次数
  maxPerDay: 100,
};
